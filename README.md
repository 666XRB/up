# 图片上传展示项目

这是一个基于纯前端技术实现的图片上传展示项目，无需后端服务器支持。

## 功能特点

- **图片上传**：支持点击上传、拖拽上传和拍照上传
- **图片池管理**：最多可上传5张图片，采用**队列（FIFO）结构**，超过时自动移除最早上传的图片
- **图片预览**：点击图片（移动端短按）可放大预览
- **图片下载**：在预览模式中可以下载图片到本地
- **数据持久化**：使用localStorage存储图片数据，刷新页面后不会丢失
- **图片删除**：桌面端右键删除，移动端长按删除
- **响应式设计**：适配不同屏幕尺寸，移动端体验优化

## 技术栈

- HTML5
- CSS3
- JavaScript (原生)
- HTML5 File API
- localStorage API

## 静态部署方法

这个项目是完全静态的，可以通过以下方式部署：

### 方法一：直接打开HTML文件

1. 在文件浏览器中找到 `index.html` 文件
2. 双击文件直接在浏览器中打开

### 方法二：使用静态文件服务器（推荐）

对于更好的性能和兼容性，推荐使用静态文件服务器：

#### 使用Python内置服务器

```bash
# Python 3.x
cd f:\Adaogu1\up
python -m http.server 8000
```

然后在浏览器中访问 `http://localhost:8000`

#### 使用Node.js的http-server

```bash
# 先安装http-server（如果尚未安装）
npm install -g http-server

# 启动服务器
cd f:\Adaogu1\up
http-server -p 8080
```

然后在浏览器中访问 `http://localhost:8080`

## 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge

## 注意事项

- 所有数据仅存储在浏览器的localStorage中，不会上传到任何服务器
- 由于localStorage的限制，不建议上传过大的图片文件
- 清空浏览器缓存或使用隐私模式可能会导致存储的图片丢失

## 项目结构

```
└── index.html  # 主页面文件
└── README.md   # 项目说明文件
```

## 版权信息

© 2023 图片上传展示项目 - 基于前端技术实现